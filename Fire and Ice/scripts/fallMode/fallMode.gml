	/// @desc fallMode(sprite)
/// @arg sprite 
if (argument[0] == jump_b1 || argument[0] == jump_b2 || argument[0] == slide_b || argument[0] == walk_b || argument[0] == crouch_b){
    sprite_index = fall_b; 
    image_speed = .6;   
}
